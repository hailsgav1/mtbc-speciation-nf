// AWS Batch - stubbed for v2. Fill in queue + S3 workDir when ready.
// process.executor = 'awsbatch'
// process.queue    = 'your-batch-queue'
// workDir          = 's3://your-bucket/work'
// aws.region       = 'us-west-2'
params {
    placeholder = 'cloud profile is a v2 stub - see README roadmap'
}
